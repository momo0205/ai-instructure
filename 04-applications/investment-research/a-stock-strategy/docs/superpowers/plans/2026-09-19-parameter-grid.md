# 动量窗口 × 持有期网格

在已有两段Study流程增加可选lookbacks数组，仅price_momentum与qlib_momentum支持。持有期2–8项沿用原约束，窗口1–8项，每项1–252且不重复；总组合不超过24。旧请求/历史兼容。成员明确保存lookback；selected_candidate保存完整组合，后段只运行此组合。收益同分优先短持有期再短窗口。所有组合及两段请求预验证。MLflow context携带组合及锁定组合。

前端按基础策略显示可选窗口输入（空值沿用原窗口），提交前提示组合数与额外一次后段任务。结果表显示窗口与持有期，旧记录缺窗口显示“沿用原参数”。保留取消、冻结行情、失败隔离。

验证：真实引擎2×2组结果/参数/后段锁定/重启；请求策略与容量拒绝；旧批次检查；前端解析及真实浏览器提交。没有三段划分、滚动或自动收益结论。

2026-09-20完成验证：27项Python、18项Node通过，JS语法和diff检查通过。真实浏览器Qlib样例2×2网格d4ffdcb1f60749e5bc6f1f421eb88994完成四组前段及一组后段，锁定持有3日/窗口5日；刷新历史与390px页面无溢出。MLflow五条FINISHED记录，唯一validation记录完整锁定组合。另一个无完整交易样例正确停在no_candidate。独立审查代理因额度限制未完成本轮审查，主代理已逐项检查参数构造、旧记录回退、完整组合锁定和恢复关联。工作台已重启加载。
