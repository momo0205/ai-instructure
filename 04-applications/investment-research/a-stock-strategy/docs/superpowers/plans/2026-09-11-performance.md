# 对照性能与进度

用户批准优化：保持100轮、种子与收益口径，减少重复准备并显示阶段进度。

- [ ] 保存真实茅台20万/3天/100轮完整基线及耗时。
- [ ] TDD 为引擎加入任务内 PreparedMarket：只校验/排序一次，缓存每日分组与市场状态；触发参数不匹配拒绝复用，策略仍仅拿截至当日的副本，共用撮合循环。
- [ ] 对照复用准备结果，不跨任务缓存。测试原始与准备执行的trades/equity/events/warnings相同。
- [ ] application/progress.py 提供作用域内回调，CLI无监听无影响。JobManager内存保存运行进度，查询输出，重启后沿用interrupted状态；不改变存储协议。阶段回调也检查取消，不继续后续轮次。
- [ ] 前端显示阶段/轮数/进度条，保留轮询与取消。
- [ ] 对比100轮全结果（排除代码审计哈希），报告实测提速。回归、浏览器、独立审查后提交。
