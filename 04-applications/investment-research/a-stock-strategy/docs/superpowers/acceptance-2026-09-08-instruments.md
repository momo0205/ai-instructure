# 标的数据管理验收

用户批准：回测仅下拉选择已准备标的，新增代码下载独立进行。本轮沿用现有分支，未修改其他项目。

已实现固定策略单选、排名策略多选、覆盖区间交集、独立后台下载、下载状态及失败重试、证券名称/交易所检查、股票仅行情标记和服务端回测限制。下载生成独立版本，不覆盖基线和旧任务快照。

2026-09-08 最终验证：163 项 Python 测试和 5 项 Node 测试通过，JS 语法与 git diff --check 通过。审查修复了日期初始化交集覆盖、未知复权默认、原始响应路径基准，以及下载完成时刷新目录的竞态。

实际验收：002015.SZ 在 2024-01-02 至 2025-12-31 的下载成功，任务 51b2c875ff3547149d785475ca762726。浏览器确认协鑫能科显示“仅行情”，回测下拉仅三只已验证 ETF；排名多选两只 ETF 提交成功，30 笔交易；复制参数正确保留两只；切换样例日期上界为 2024-05-06；错误代码 002015.SH 明确提示 .SZ；浏览器错误日志为空。

新版本服务已启动在 http://127.0.0.1:8765 。数据和下载历史仅保存在本地，未加入 Git。

边界：当前仅三只已验证 ETF 支持回测；下载区间受真实基线限制，每次版本从 real 基线构建。更新已有 ETF 时整支替换成所选区间，避免拼接不同复权基准。新增股票交易模型仍待后续实现。早期验收下载版本生成于来源路径修复之前，其行情有效但来源路径记录沿用旧实现；后续下载使用修复后的清单格式。
