# 本机工作台验收记录

实现固定标的、横截面排名两种策略的参数表单，后台串行回测、任务历史、取消、复制参数、JSON 导出，以及收益指标、净值、回撤和逐笔交易展示。策略构造器与参数定义共用注册入口。代码注释说明交易时点、预热、快照和任务状态约束。

验证：

- 完整 Python 测试、JavaScript 语法检查和 git diff 空白检查。
- 两种策略真实数据 API 任务已完成；固定策略原始单日持有任务 33 笔，排名策略 30 笔，与此前 CLI 结果一致。
- Chrome 已加载真实数据、历史任务、指标、图表节点和逐笔交易。未完成全部浏览器交互及控制台验收，不将接口测试等同于完整 UI 自动化。
- 正式腾讯下载命令实测 2025-12-29 至 2025-12-31，四个标的共 12 行；输出到临时目录，未覆盖研究数据。
- 新增回归覆盖清单哈希错误、提交时数据变化、任务摘要不读取结果列、指数日期边界及单一策略注册入口。

本机入口：http://127.0.0.1:8765 。重启命令为项目目录下 `.venv/bin/python -m strategy serve`。任务保存在 `reports/workbench`，数据及任务结果不提交到代码仓库。

当前边界：单用户、日频、单持仓 ETF 研究；新策略仍需编写 Python，并遵循现有引擎与标的参数约定。数据刷新通过 CLI。取消运行中的任务会丢弃结果，但等待本次计算结束后才处理下一任务。前复权及未知停牌/涨跌停状态的限制会显示在结果中。历史无哈希清单只能保存审计副本，无法追溯证明其下载时一致性；新下载清单包含内容哈希，不匹配则拒绝运行。更新中断后可能需要重新下载以恢复匹配的数据与清单。

Mem0 回写尝试失败：环境缺少 DEEPSEEK_API_KEY；本文件提供交接依据。
